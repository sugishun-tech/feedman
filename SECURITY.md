# セキュリティとプライバシー

feedman はローカルのデスクトップアプリケーションです。サーバー、クラウド同期、解析用テレメトリー、プラグイン実行機能は持ちません。

## 入力と通信

フィード URL と本文中で開くリンクは HTTP / HTTPS に限定します。HTTPS の証明書検証は libcurl の既定を維持し、検証無効化オプションは提供しません。HTTP自体は暗号化されないため、配信元が対応する場合はHTTPSを使ってください。

リダイレクトは最大5回で、移動先もHTTP(S)に限定します。libcurlの通常のプロキシ環境変数に従います。URLに認証情報やトークンが含まれる場合、設定ファイルとDBに保存される可能性があります。これらを共有・コミットしないでください。通常の進捗・取得失敗メッセージにはURL全体ではなくホスト名を使いますが、OSやプロキシ側のログまで秘匿する機能ではありません。

ローカル / プライベートネットワークのHTTP(S)フィードも、利用者が設定すれば取得できます。任意の第三者に `feeds.txt` を書かせる運用や、信頼できない利用者向けのウェブサービスにそのまま転用する運用は想定していません。外部ブラウザーでリンクを開いた後の安全性はブラウザー側にも依存します。

## XMLと本文

XMLは `NONET` で読み、外部エンティティーローダーも全スキームについて無効化します。DTDを含むフィードは拒否します。`NOENT`、`DTDLOAD`、`HUGE` は使いません。内蔵ビューアーはGtkTextViewであり、JavaScript、外部画像、CSSやiframeのネットワーク取得を行いません。

既定の上限は1フィードの展開後16MiB、1記事本文2MiB、5万記事/フィード、1万フィード、64ワーカーです。接続タイムアウトは10秒、全体タイムアウトは既定30秒で `--timeout 1..300` により変更できます。上限はDoS耐性の完全な保証ではありません。大量のフィードや巨大なXMLでは、ワーカー数に応じてメモリーも増えるため並列数を下げてください。

## 保存

SQLの検索語、タグ、URL、本文などの値はバインドします。新規の個人データディレクトリーは0700、CLI起動時のumaskは0077を使用します。既存ディレクトリーのアクセス権やディスク暗号化までは変更しません。

DBはローカルディスク向けのWAL / synchronous=NORMALです。突然の電源断では直前の更新を失う可能性があります。重要な記事は別途バックアップしてください。元記事の削除・改変、配信元のRSS仕様変更、配信終了に対する完全保存や継続取得は保証しません。

## 報告

公開リポジトリーの管理者は、公開前にGitHubのPrivate vulnerability reportingを有効にするか、非公開の報告窓口を用意してください。現時点で同梱コードが送信先を自動作成することはありません。認証情報、非公開フィード、購読履歴を公開Issueへ添付しないでください。
